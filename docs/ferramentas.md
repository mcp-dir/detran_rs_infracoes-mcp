# Ferramentas

DETRAN RS: Infrações expõe 1 ferramenta (todas somente leitura).

### 1. `detran_rs_infracoes_consultar`
**Input**: `placa`, `renavam` (opcional), `cnpj` (opcional), `guia_pagamento` (opcional), `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

DETRAN RS: Infrações, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar DETRAN RS: Infrações
```
